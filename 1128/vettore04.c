#include<stdio.h>
#include<stdlib.h>
#include<time.h>
#define N 30
int main(){
	int somma=0;
	int ngiorni[30];
	int tmin=-6;
	int tmax=16;
	int giornimax0=0;
	int media=0;
	srand(time(0));
	for(int i=0; i<30; i++){
		ngiorni[i]=rand()%21-5;
		if(valori[i]<valori[i-1]):
			tmin=valori[i];
		if(valori[i]>valori[i+1]):
			tmax=valori[i];
		if(valori[i]>0):
			giornimax0+=1;
		somma+=valori[i];
	}
	media=somma/N;
	printf("La temperatura massima è: ");
	printf



return 0;
}
